const styles = theme => ({
  selectWrapper: {
    '& .MuiSelect-root': {
      backgroundColor: 'transparent',
      fontWeight: 500,
      textTransform: 'inherit',
    },
    '& .MuiSelect-icon': {
      color: '#CDCDE4',
    },
  },
});

export default styles;
