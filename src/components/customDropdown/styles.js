const styles = (theme) => ({
    selectWrapper: {
        '& .MuiSelect-root': {
            fontWeight: 500,
            fontSize: '15px',
            color: '#CDCDE4',
        },
        '& .MuiSelect-icon': {
            color: '#CDCDE4',
        },

    }
})

export default styles;