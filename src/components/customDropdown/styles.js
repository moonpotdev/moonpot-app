const styles = (theme) => ({
    selectWrapper: {
        '& .MuiSelect-root': {
            fontWeight: 500,
            fontSize: '12px',
            color: '#ffffff',
        },
        '& .MuiSelect-icon': {
            color: '#ffffff',
        },
        '& .MuiInput-underline:before': {
            borderColor: '#ffffff',
        },
    }
})

export default styles;