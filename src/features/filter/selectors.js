export const selectFilterConfig = state => state.filter.config;
export const selectFilterConfigLoaded = state => state.filter.configLoaded;
export const selectFilterMode = state => state.filter.mode;
export const selectFilterIds = state => state.filter.ids;
